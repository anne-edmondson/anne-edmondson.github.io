---
interface Props {
  title: string;
  capabilities: Array<string>;
}

const { title, capabilities } = Astro.props;
---

<article>
  <h3>{title}</h3>
  <ul>
    {capabilities.map((capability) => <li>{capability}</li>)}
  </ul>
</article>
